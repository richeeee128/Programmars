const arr = [4, 3, 2, 1];

function solution(arr) {
  var answer = arr.sort();
  if (Number.isInteger(answer) == String()) {
    answer.shift();
  } else {
    return (new arr = [-1]);
  }
  return answer;
}
